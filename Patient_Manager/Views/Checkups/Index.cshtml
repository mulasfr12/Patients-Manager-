@model IEnumerable<Patient_Manager.Models.Checkup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Checkuptype)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Checkupdate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Checkuptime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Patient)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Checkuptype)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Checkupdate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Checkuptime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patient.Patientid)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Checkupid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Checkupid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Checkupid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
